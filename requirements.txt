# Core trading + data SDK (official, current — NOT the deprecated alpaca-trade-api)
alpaca-py>=0.42,<0.44

# LLM analysis — Ollama (local, free, default) and/or Claude
ollama>=0.3
anthropic>=0.110,<0.113   # only needed if AI_PROVIDER=anthropic

# Timezone data (fixed EOD/ET logic via stdlib zoneinfo; required on Windows)
tzdata>=2024.1

# Free fundamentals: full-market volume + share float (low-float filter)
yfinance>=1.4,<1.5

# Scheduling (7 AM premarket start)
APScheduler>=3.10,<4.0

# Web dashboard
Flask>=3.0,<3.2

# Config & utilities
python-dotenv>=1.0,<2.0
pydantic>=2.9,<3.0
pandas>=2.2

# Testing
pytest>=8.0
